# Default port for vite client is 5173
CLIENT_URL=http://localhost:5173

API_HOST=http://localhost
API_PORT=9090

# The URL of our Auth0 Tenant Domain.
AUTH0_DOMAIN=

# Auth0 application's Client ID.
AUTH0_CLIENT_ID=

# Auth0 application's Client Secret.
AUTH0_CLIENT_SECRET=

DB_PORT=5432
DB_HOST=todo_db
DB_USER=
DB_PASSWORD=
DB_NAME=localhost #while using container change from localhost to container name i.e. todo_db.
DB_CONNECTION_STRING="host=${DB_HOST} user=${DB_USER} password=${DB_PASSWORD} dbname=${DB_NAME} sslmode=disable"